{
  "name": "localdoc-intel",
  "private": true,
  "version": "1.1.0",
  "description": "Local-first AI document intelligence app with cited RAG answers.",
  "scripts": {
    "dev": "npm --prefix frontend run dev",
    "build": "npm --prefix frontend run build",
    "lint": "npm --prefix frontend run lint",
    "test": "npm --prefix frontend run test"
  },
  "engines": {
    "node": ">=24.15"
  },
  "license": "MIT"
}
